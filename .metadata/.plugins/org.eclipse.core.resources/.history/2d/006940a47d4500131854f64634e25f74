object Problem6 {
  def sumOfTheSquares (n: Int) = (1 to n).map(x => x*x).sum
                                                  //> sumOfTheSquares: (n: Int)Int
  def squareOfTheSum (n: Int) = (1 to n).sum      //> squareOfTheSum: (n: Int)scala.collection.immutable.Range.Inclusive
}