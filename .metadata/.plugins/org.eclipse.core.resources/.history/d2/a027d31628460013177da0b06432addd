object Problem10 {
	lazy val primeN: Stream[Int] = 2 #:: Stream.from(3).filter(i => primeN.takeWhile(j => j * j <= i).forall(i % _ > 0))
	
	def sumOfNPrimes(n: Int) = {
	
	def innerSumOfNPrimes(n: Int, sumAcc: Int)
	
	}
}