object Problem4 {
	val biggest3numbersPalindrome = (100 to 999).view.map(i => (i to 999).map(i *)).filter(n => n.toString == n.toString.reverse).max
                                                  //> biggest3numbersPalindrome  : Int = 906609
}